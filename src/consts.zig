pub const num = @import("consts/num.zig");
pub const cgs = @import("consts/cgs.zig");
pub const cgsm = @import("consts/cgsm.zig");
pub const mks = @import("consts/mks.zig");
pub const mksa = @import("consts/mksa.zig");

test {
    _ = num;
    _ = cgs;
    _ = cgsm;
    _ = mks;
    _ = mksa;
}
